export const HOST_SERVE = "http://localhost:8080";
const API = "/api";

export const Endpoint = {
  PAYMENT_BOOKING: HOST_SERVE + API + "/bookings",
  PAYMENT_PROMOTION_CHECK: HOST_SERVE + API + "/promotions/check",
  PAYMENT: HOST_SERVE + API + "/payments",
  PROMOTION_USECOUNT: HOST_SERVE + API + "/promotions/use-count",
  REVIEW: HOST_SERVE + API + "/reviews",
};
